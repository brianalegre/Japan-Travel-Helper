const mongoose = require('mongoose');
const { Schema } = mongoose;

const ModelNameSchema = new Schema({

});

const ModelNameSchema = mongoose.model('ModelName', ModelNameSchema);

module.exports = ModelNameSchema
